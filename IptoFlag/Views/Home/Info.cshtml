@{
    ViewBag.Title = "Info Entries";
}

@*
@foreach (var info in ViewBag.Infos as List<IptoFlag.Info>)
{
    <p> @info.IP </p>
}
*@

@* 
@using (Html.BeginForm())
{   
    <p>Country: @Html.DropDownList("IdCountry", "All")  
    <input type="submit" value="Filter" /></p>
}
*@



@(Html.Kendo().Grid<IptoFlag.Info>()
          .Name("grid")
          .DataSource(dataSource =>
          {
              dataSource.Ajax().PageSize(2)
              .Read(read => read.Action("ReturnInfos", "Home"));
          })
          .Columns(columns =>
          {
              columns.Bound(ec => ec.IdInfo).Title("ID Info");
              columns.Bound(ec => ec.IdCountry).Title("ID Country");
              columns.Bound(ec => ec.InfoCountry).Title("Information");
              columns.Bound(ec => ec.IP).Title("IP");
              columns.Bound(ec => ec.Location).Title("Location");
              columns.Bound(ec => ec.Date).Title("Date").Format("{0:yyyy/MM/dd}");
          })
          .Selectable(selection => selection.Mode(GridSelectionMode.Single))
          .Sortable()
          .Filterable()
          .Pageable()
     )

